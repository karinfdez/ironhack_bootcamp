
	<h1><%= @user.first_name%><spam><%= @user.last_name%></spam></h1>

	<p>Contact email: <%=@user.email%></p>
	<p>Description of artist: FALTA(Migration doesn't work)</p>

	<% if current_user.present? && current_user.admin?%>
		
		<a href='/users/<%=@user.id%>/posts/new'>Create new post</a>
		
	<% end %>
	
