<%= form_for @image_scaffold, html: { multipart: true } do |f| %>
  <% if @image_scaffold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_scaffold.errors.count, "error") %> prohibited this image_scaffold from being saved:</h2>

      <ul>
      <% @image_scaffold.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
